{
  "version": 2,
  "builds": [
    {
-     "src": "package.json",
+     "src": "src/frontend/package.json",
      "use": "@vercel/static-build",
-     "config": { "distDir": "dist" }
+     // tady „dist“ znamená složku „dist“ uvnitř src/frontend
+     "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
