{
  "version": 2,
  "builds": [
    {
-     "src": "src/frontend/package.json",
-     "use": "@vercel/static-build",
-     "config": { "distDir": "dist" }
+     "src": "src/frontend/package.json",
+     "use": "@vercel/static-build",
+     "config": { "distDir": "build" }
    }
  ]
}

